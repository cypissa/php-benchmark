# PHP Benchmark

Simple benchmark across php versions and hhvm

# How it works

It is based on Docker containers. It creates one containers per php version and runs benchmark on them.

# Usage

```
    $ git clone https://github.com/cypiszzz/php-benchmark
    $ cd php-bechmark
    $ ./rebuild.sh
    $ ./run.sh
```
